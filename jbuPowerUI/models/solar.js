class Solar {
  constructor(current_power, energy_today, energy_lifetime, status, time) {
    this.current_power = current_power,
    this.energy_today = energy_today,
    this.energy_lifetime = energy_lifetime,
    this.status = status,
    this.time = time
  }
};

export default Solar;
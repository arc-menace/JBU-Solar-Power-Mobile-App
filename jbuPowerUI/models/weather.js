class Weather{
  constructor(description, temp, wind_speed, clouds, time){
    this.description = description,
    this.temp = temp,
    this.wind_speed = wind_speed,
    this.clouds = clouds,
    this.time = time
  }
};

export default Weather;